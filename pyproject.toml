[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "emf-inspector"
version = "1.1.0"
description = "AI-powered electromagnetic interference estimation tool for KiCad PCB layouts"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
    { name = "Atharva M" },
]
keywords = [
    "PCB", "EMI", "electromagnetic", "KiCad",
    "signal-integrity", "RF", "EMC"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Electronic Design Automation (EDA)",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy>=1.24",
    "matplotlib>=3.7",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.1",
]

[project.scripts]
emf-inspector = "emf_inspector.__main__:main"

[project.urls]
Homepage = "https://github.com/ATHARVA316-DEV/kicad-emf-inspector"
Repository = "https://github.com/ATHARVA316-DEV/kicad-emf-inspector"
Issues = "https://github.com/ATHARVA316-DEV/kicad-emf-inspector/issues"
Documentation = "https://github.com/ATHARVA316-DEV/kicad-emf-inspector#readme"

[tool.setuptools.packages.find]
where = ["."]
include = ["emf_inspector*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py310"
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Line length handled by formatter, not linter
